name: lucky_wheel
description: "Spinly Wheel Decision - A state-driven random experience engine."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.3.3
  webview_flutter: ^4.10.0
  url_launcher: ^6.3.1
  adjust_sdk: ^4.38.0
  crypto: ^3.0.6
  http: ^1.2.2
  encrypt: ^5.0.3
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
